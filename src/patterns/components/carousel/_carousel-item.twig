<li class="carousel-item {{ modifier }}" {{ extra_attributes }}>
  {#
    Optional: Pass in Drupal specific functionality.
    Note that this is only needed with nodes and blocks.
    This isn't needed for Drupal Paragraphs.
  #}
  {#
    {{ title_prefix }}
    {{ title_suffix }}
  #}

  {% if image %}
    <div class="carousel-item__media">
      {%
        include '@training_theme/media-item/media-item.twig' with {
          "image": image,
        } only
      %}
    </div>
  {% endif %}

  {% if title %}
    <div class="carousel-item__content">
      {%
        include '@training_theme/heading/heading.twig' with {
          "title": title,
          "heading_level": 3,
          "modifier": "carousel-item__title",
        } only
      %}
      {% if button_text %}
        {%
          include '@training_theme/button/button.twig' with {
            "text": button_text,
            "url": url
          } only
        %}
      {% endif %}
    </div>
  {% endif %}
</li>
